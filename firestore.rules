rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      // Allow write only if the user is authenticated AND has the specific email
      allow write: if request.auth != null && request.auth.token.email == 'giromon415@gmail.com';
    }
  }
}
